<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ChefIA" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Style for the system splash screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.ChefIA</item>

        <!-- Professional trick: makes the starting window translucent to skip the OS drawing a splash -->
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>